---
import Layout from '../layouts/Layout.astro';
export const prerender = false; // This page is dynamic based on purchase
---

<Layout>
  <main class="bg-brand-white min-h-screen py-16 md:py-24 px-6">
    <div class="container mx-auto max-w-3xl">

      <!-- Success Message -->
      <section class="text-center mb-16">
        <div class="inline-flex items-center justify-center w-20 h-20 bg-brand-lime rounded-full mb-6">
          <svg class="w-12 h-12 text-brand-black" fill="none" viewBox="0 0 24 24" stroke-width="2.5" stroke="currentColor">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5" />
          </svg>
        </div>
        <h1 class="text-4xl md:text-5xl font-serif text-brand-black mb-4">
          Thank You! The Journey Begins.
        </h1>
        <p class="text-xl font-sans text-brand-black/70">
          You've given an incredible gift. Here is what you can expect next.
        </p>
      </section>

      <!-- What Happens Next -->
      <section class="bg-white border border-black/10 rounded-2xl shadow-lg p-8 md:p-12 mb-12">
        <h2 class="text-3xl font-serif text-brand-black mb-8 text-center">What Happens Next</h2>
        <div class="space-y-8">
          <!-- Step 1 -->
          <div class="flex items-start gap-4">
            <div class="shrink-0 w-10 h-10 bg-brand-navy text-brand-white rounded-full flex items-center justify-center font-bold font-sans">1</div>
            <div>
              <h3 class="text-xl font-bold font-sans mb-1">Check Your Email</h3>
              <p class="font-sans text-brand-black/80">
                You'll receive a confirmation email with your purchase details and a link to your digital gift certificate within the next few minutes.
              </p>
            </div>
          </div>
          <!-- Step 2 -->
          <div class="flex items-start gap-4" id="physical-delivery-step">
            <div class="shrink-0 w-10 h-10 bg-brand-navy text-brand-white rounded-full flex items-center justify-center font-bold font-sans">2</div>
            <div>
              <h3 class="text-xl font-bold font-sans mb-1">Gift Certificate Delivery</h3>
              <p class="font-sans text-brand-black/80" id="delivery-message">
                If you chose a physical certificate, it is being prepared and will be shipped within 2 business days. Please allow 5-7 days for delivery.
              </p>
            </div>
          </div>
          <!-- Step 3 -->
          <div class="flex items-start gap-4">
            <div class="shrink-0 w-10 h-10 bg-brand-navy text-brand-white rounded-full flex items-center justify-center font-bold font-sans">3</div>
            <div>
              <h3 class="text-xl font-bold font-sans mb-1">Personal Discovery Session</h3>
              <p class="font-sans text-brand-black/80">
                Our team will reach out to the storyteller's provided email address within 2 business days to schedule their warm, personal discovery session.
              </p>
            </div>
          </div>
          <!-- Step 4 -->
          <div class="flex items-start gap-4">
            <div class="shrink-0 w-10 h-10 bg-brand-navy text-brand-white rounded-full flex items-center justify-center font-bold font-sans">4</div>
            <div>
              <h3 class="text-xl font-bold font-sans mb-1">The Journey Begins</h3>
              <p class="font-sans text-brand-black/80">
                The storyteller will receive their personal welcome email and can begin their conversations with Peg as soon as they're ready.
              </p>
            </div>
          </div>
        </div>
      </section>

      <!-- Support Section -->
      <section class="text-center bg-gray-100 rounded-2xl p-8 mb-12">
        <h2 class="text-2xl font-serif text-brand-black mb-4">Questions or Need Support?</h2>
        <p class="font-sans text-brand-black/70 mb-6">
          Our team is here to help you every step of the way.
        </p>
        <a href="mailto:support@storysmith.work" class="inline-block bg-brand-navy text-brand-white font-sans uppercase tracking-wider py-3 px-8 hover:bg-opacity-90 transition-colors rounded-lg">
          Contact Support
        </a>
      </section>

      <!-- The Promise -->
      <section class="text-center border-2 border-brand-lime rounded-2xl p-8 bg-lime-50/50">
        <div class="flex justify-center mb-4">
            <svg class="w-12 h-12 text-brand-lime" fill="none" viewBox="0 0 24 24" stroke-width="1.5" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75 11.25 15 15 9.75M21 12a9 9 0 1 1-18 0 9 9 0 0 1 18 0Z" />
            </svg>
        </div>
        <h3 class="text-2xl font-serif text-brand-black mb-3">Our "First Conversation" Promise</h3>
        <p class="font-sans text-brand-black/70">
          If the storyteller doesn't love their first conversation with Peg, we'll refund your purchase in full. No questions asked.
        </p>
      </section>

      <!-- Return Home -->
      <div class="text-center mt-16">
        <a href="/" class="text-brand-navy font-bold uppercase tracking-wider font-sans hover:underline">
          ← Return to Homepage
        </a>
      </div>
    </div>
  </main>
</Layout>

<script>
  // This script will dynamically hide the physical delivery step if not applicable.
  document.addEventListener('DOMContentLoaded', () => {
    const urlParams = new URLSearchParams(window.location.search);
    // You will need to pass this parameter from your Stripe success URL
    const giftingType = urlParams.get('gift_type'); 
    const physicalDeliveryStep = document.getElementById('physical-delivery-step');

    if (giftingType === 'digital' && physicalDeliveryStep) {
      physicalDeliveryStep.style.display = 'none';
    }
  });
</script>